# Run with `dart run ffigen --config ffigen.yaml`.
name: RustOhosAppBindings
description: |
  Bindings for `src/rust_ohos_app.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/rust_ohos_app_bindings_generated.dart'
headers:
  entry-points:
    - 'src/rust_ohos_app.h'
  include-directives:
    - 'src/rust_ohos_app.h'
comments:
  style: any
  length: full
